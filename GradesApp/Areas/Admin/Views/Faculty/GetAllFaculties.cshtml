@model IEnumerable<ApplicationUser>

<div class="container">
    <div class="row pt-4 pb-4">
        <div class="col-6">
            <h2 class="text-primary">
                Faculty List
            </h2>
        </div>
        <div class="col-6 text-end">
            <a asp-area="Admin" asp-controller="Faculty" asp-action="Upsert" class="btn btn-primary">
                <i class="bi bi-plus-lg"></i> Create Faculty
            </a>

        </div>
    </div>
    <table id="tblData" class="table table-bordered table-striped" style="width:100%">
        <thead>
            <tr>
                <th>
                    Name
                </th>
                <th>
                    Abbreviation
                </th>
                <th>

                </th>
            </tr>
        </thead>
    </table>
</div>

@section Scripts {
    @{
        var randomParam = new Random().Next(10000);
    }
    <script src="~/js/admin/faculty/faculty.js?cache=@randomParam"></script>
}
